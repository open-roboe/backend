apiVersion: apps/v1
kind: Deployment
metadata:
  name: roboapp
spec:
  selector:
    matchLabels:
      app: roboapp
  revisionHistoryLimit: 3
  replicas: 1
  template:
    metadata:
      labels:
        app: roboapp
    spec:
      containers:
        - name: roboapp
          image: ghcr.io/open-roboe/backend:sha-213be43115e3d8eab8556d65ca3b2cb42c2a334b
          env:
            - name: SQLITE_FILE
              value: test.db
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: roboapp-secrets
                  key: SECRET_KEY
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
